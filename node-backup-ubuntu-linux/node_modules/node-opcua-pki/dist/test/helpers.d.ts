import { ErrorCallback } from "../lib";
export declare function grep(data: string, regExp: RegExp): string;
interface TestData {
    tmpFolder: string;
}
export declare function beforeTest(self: any, f?: (callback: ErrorCallback) => void): TestData;
export {};
